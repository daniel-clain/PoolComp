# Production or custom dev: full WebSocket URL (bypasses Vite proxy). Example:
# VITE_WS_URL=ws://localhost:3000/ws

# Local dev (npm run vite): proxy /ws to backend. Default 3000 — match backend PORT.
# VITE_DEV_BACKEND_PORT=3000
