{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "install:shared": "npm --prefix ../shared install",
    "predev": "npm run install:shared",
    "dev": "vite",
    "pretypecheck": "npm run install:shared",
    "typecheck": "tsc --noEmit",
    "prebuild": "npm run install:shared",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "sass": "^1.98.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.1",
    "vite-plugin-checker": "^0.14.5"
  },
  "dependencies": {
    "@fontsource/oswald": "^5.2.8",
    "@types/lodash": "^4.17.24",
    "date-fns": "^4.4.0",
    "lodash": "^4.18.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "vitest": "^4.1.5"
  }
}